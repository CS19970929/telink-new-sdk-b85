<Window x:Class="BmsTool.Android.Deployer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Android 固件发送器" Width="760" Height="650" MinWidth="680" MinHeight="560"
        WindowStartupLocation="CenterScreen" AllowDrop="True" Drop="Window_Drop">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="Android 固件发送器" FontSize="26" FontWeight="SemiBold" Foreground="#244A36" />
            <TextBlock Margin="0,8,0,0" TextWrapping="Wrap"
                       Text="选择 PC 已编译的正式 BIN，点击一次即可安全导入已连接手机并打开 BMS Tool。不会自动开始 OTA。" />
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#D4DED7" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="110" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="已连接手机" FontWeight="SemiBold" />
                <ComboBox x:Name="DeviceCombo" Grid.Column="1" Height="34" Margin="0,0,10,0" DisplayMemberPath="DisplayName" />
                <Button Grid.Column="2" Height="34" Content="刷新设备" Click="RefreshDevices_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="#D4DED7" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="110" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="正式固件 BIN" FontWeight="SemiBold" />
                <TextBox x:Name="FirmwarePathBox" Grid.Row="0" Grid.Column="1" Height="34" Margin="0,0,10,0"
                         VerticalContentAlignment="Center" TextChanged="FirmwarePathBox_TextChanged" />
                <Button Grid.Row="0" Grid.Column="2" Height="34" Content="选择文件" Click="BrowseFirmware_Click" />
                <TextBlock x:Name="FirmwareInfoText" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                           Margin="0,10,0,0" Text="也可以把 BIN 直接拖到窗口" TextWrapping="Wrap" Foreground="#53665A" />
            </Grid>
        </Border>

        <Button x:Name="SendButton" Grid.Row="3" Height="48" Margin="0,0,0,10"
                Content="发送到手机并打开 BMS Tool" FontSize="16" FontWeight="SemiBold"
                Background="#2E6B4B" Foreground="White" Click="SendFirmware_Click" />

        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <ProgressBar x:Name="SendProgress" Height="10" Minimum="0" Maximum="100" />
            <TextBlock x:Name="StatusText" Margin="0,8,0,0" Text="正在检查 ADB…" TextWrapping="Wrap" />
        </StackPanel>

        <TextBox x:Name="LogBox" Grid.Row="5" IsReadOnly="True" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12" TextWrapping="NoWrap" />
    </Grid>
</Window>
