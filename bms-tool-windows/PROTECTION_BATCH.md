# 软件保护批量写入修复

旧逻辑逐字段0x06写入，每次由固件校验完整参数；最终合法但中间不合法的三级组合会返回0x03。新逻辑预读65字、合并修改并校验最终组合，每个项目5字0x10提交后回读，19字节请求适配MTU23。不修改固件协议。

支持同时修改多个项目乃至全部13组；每组原子，跨组依次提交。失败提示当前项目、地址、此前已确认组数，当前组可能已提交，需重新读取确认。不自动回滚已完成项目。真正非法的阈值顺序、三级恢复关系、温度范围仍会在发写命令之前拒绝。两版复用Shared/ProtectionBatch.cs和BmsClient通信锁。

验证：test-protection-batch.ps1通过中间非法/最终合法、多组与13组、19字节帧、非法候选零写入、部分失败；test-afe-fragments.ps1回归通过。客户版与内部测试版已使用build-release.ps1编译，ReleaseTag=protection-batch-20260917；既有net7框架产生NETSDK1138生命周期警告，本次不更换框架。

EXE SHA256：
- 客户版：7D20A550186CE9961C83302C990F35C152FE7BE1D10F226E17185AF130C20070
- 内部版：9CAE6A67ACBCEFFAE475DB4B2B2C0F6B1BA6D63964D337A40F72CC509E603630

截图未包含设备参数/请求帧；本次确认并修复源码中的写入顺序问题，不宣称已用该用户实板复现所有0x03来源。未连接设备执行参数写入。
