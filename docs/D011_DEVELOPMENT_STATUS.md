# D011 开发状态与未完成清单

更新日期：2026-09-13。目标：HS-D011 / TLSR8251F512ET32 / SH3673510 / 10S。
硬件依据：[D011_HARDWARE_REFERENCE.md](D011_HARDWARE_REFERENCE.md)。

**当前结论：已形成 D011 软件集成并通过远端 Windows TC32 构建；尚未完成安全逻辑闭环与实板验收，不能称为量产完成。**

## 1. 远端基线和本次结果

- 原 SPI 基础分支：`feature/sh3673520-spi`，`d1ad59463025d9db1ebae2bf3a8a9bb62b30ee8e`。
- 本次接手的 D011 分支：`feature/sh3673510-d011-bms`，`cf1d92e5edca66a616e3b54406c83b41a54d90e3`；相对原 SPI 基础有 14 个提交。
- 原 D011 [CI 34728424941](https://github.com/CS19970929/telink-new-sdk-b85/actions/runs/34728424941)：Host 成功，Windows TC32 链接失败，缺少 `__muldi3` / `__divdi3`。
- 本次修复提交：[ec0e913d565dd2d7a32d2d9115a9b81ea2f319db](https://github.com/CS19970929/telink-new-sdk-b85/commit/ec0e913d565dd2d7a32d2d9115a9b81ea2f319db)。
- 修复后的 [CI 34729110796](https://github.com/CS19970929/telink-new-sdk-b85/actions/runs/34729110796)：Host contract checks 与 TC32 production build 均成功。
- Artifact：`telink-bms-ec0e913d565dd2d7a32d2d9115a9b81ea2f319db`，843484 bytes，CI 保留期有限；正式发布应另行归档。

这里的 CI 结论严格对应上述代码提交；后续文档或源码提交须检查其自己的 CI，不能把历史成功套用到新 HEAD。

## 2. 本次实际完成

| 项目 | 结果 | 兼容性与边界 |
|---|---|---|
| D011 原理图转结构化 Markdown | 已完成；含来源 SHA-256、MCU/AFE、GPIO、采样、温度、供电、RS485、功率、待确认项 | 文档不替代原始 PDF 和器件手册 |
| AI 维护入口 | AGENTS.md 增加本分支 D011 说明与文档索引 | DVC1124 专属硬件事实不能套用 D011 |
| TC32 电流换算链接阻塞 | 用有边界证明的 32-bit 定点表达式替换 64-bit 乘除 | 既有公式与正负截断结果保持一致，不更换工具链或 ABI |
| 批量寄存器写入长度 | 运算前限制到可写窗口长度，防止长度缩窄/地址加法回绕 | 合法请求保持逐寄存器写入，畸形长度先拒绝且不访问缓冲区/SPI |
| 回归测试 | 全部 65536 个 signed16 ADC 值 × 16 个 Rsense，与 host-only 64-bit 真值逐项比较 | 1048576 组精确数值比较；host oracle 不进入固件 |
| 边界测试 | 无效 ADC、零 Rsense、空指针、输出保留、超长写入、全可写窗口 | 保留既有协议/初始化/采样测试 |
| 远端生产构建 | sources、clean rebuild、check-fw、size、map、manifest、verify、cppcheck 步骤成功 | 编译与分析流程成功不等于没有静态诊断，更不等于硬件正确 |

数值证明：`100000000 = 3433 * 29127 + 7009`；`abs(raw) <= 32768`。
两个乘积最大为 112492544、229670912，缩放结果最大 112500429，均在 uint32_t 范围。
利用正整数 `floor(floor(n/a)/b) = floor(n/(a*b))`，最后恢复符号，保持 C 向零截断。
测试中的 250uOhm 对应 D011 配置；测试其他阻值只用于 API 边界验证，不是新板型授权。

## 3. 接手时已经存在的功能代码

“已有代码”只描述源码状态，不代表功能已验收。

| 能力 | 已有内容 | 完成等级 |
|---|---|---|
| D011 板型选择 | D11 / 10S / 250uOhm / TLSR GPIO 表、SH3673510 backend | 有配置，关键 GPIO 已与原图对照 |
| AFE SPI | B85 硬件 SPI、CRC、帧检查、有限重试、超时、统计、复位与探测 | Host 协议测试通过，实板波形待验证 |
| 测量 | 10 路电芯、VTOP、VADC/CADC、4 路温度原始值、BMS 报告适配 | 有采样实现，精度/通道/极性/NTC 待实测 |
| 软件保护 | 单体/总压、充放电电流、温度、压差，分级滤波与故障历史 | 有实现，边界与状态联动未完成验收 |
| AFE 硬件保护 | OV/UV/OCD1/OCD2/OCC/SC/温度参数写入与读回 | 有实现，SH3673510 量化和产品阈值仍须核对 |
| MOS | SCONF2 FET 设置、BSTATUS 实际状态、输出总许可 | 有实现，存在请求语义和错误门控缺口 |
| 均衡 | 10S 掩码、充电/压差条件、定期刷新 | 有实现，控制/热/采样互扰待验证 |
| 加热 | PB4/PB5 操作、低温开启/恢复阈值、部分故障禁止 | 有实现，F1/加热组合和保护链未确认 |
| RS485 | PC2/PC3 UART DMA、PA1 方向、等 TX 完成释放 | 有实现，C-3V3 供电与 PD3 唤醒流程未闭环 |
| 低功耗 | AFE sleep/wake、部分 GPIO wake、继承 MCU deep sleep | 有路径，不能宣称安全休眠已经实现完成 |
| BLE / Modbus / SOC / Flash / OTA | 复用已有应用，并隔离 DVC 专属诊断范围 | 继承框架，D011 端到端兼容性未完成 |
| CI | Host 测试、D011 集成检查、Windows TC32 固件产物 | 已通过指定提交的自动检查 |

D011 集成测试当前主要检查源码契约/常量与调用存在；不能将其描述为执行了真实保护状态机、GPIO、AFE 或 RS485 的功能测试。

## 4. 必须处理的代码与证据缺口

优先级说明：P1 为上板功能测试/发布前必须处理，P2 为后续完善。以下为本次局部源码与原图对照发现，不是完整安全审计。

| ID | 优先级 | 当前证据与影响 | 完成判据 |
|---|---|---|---|
| D011-001 | P1→已修复(待实板) | FET 请求曾被丢弃 | 已改为“上层请求 × 输出许可 × 有效快照 × AFE/通信健康 × 方向保护”的统一仲裁；显式关断不再被底层重算覆盖 |
| D011-002 | P1→已修复(待实板) | 旧代码通信失败后仍可能被其他调用链重新开 MOS | 已增加 output inhibit；SPI/CRC/采样失败立即关 FET，重新初始化后需连续 3 个完整有效快照才解除 inhibit |
| D011-003 | P0→已修复(待实板) | PB5/HT-RF-EN 已确认是加热回路保险丝熔断触发；旧 RF_EN 路径和普通 heater 同写会造成不可逆误熔断风险 | 已删除旧 RF_EN/CHG_IN 等板级别名；普通 heater 只控制 PB4；PB5 运行路径只强制 LOW。真正熔断状态机在硬件判据完整验证前保持禁用 |
| D011-004 | P1 | sh3673510_control_init() 将 CMNT-EN 拉低；modbus_uart_init() 只配置方向与 UART，未在该入口使能 C-3V3；PD3 目前只有配置常量 | 追完整供电所有权，确认供电开启/稳定/关闭、收发、总线唤醒状态机；实测 U2 电源 |
| D011-005 | P1 | app_deepsleep_pad_wakeup_active() 只看 CHG_IN_PIN，而 D011 把它别名到 PA0；AFE 初始化另启用 PB1/PC0/PC1，PD3 通信唤醒未见接入 | 检查所有已启用源的实时电平与去抖，AFE 拒绝 sleep 能阻止 MCU sleep；验证重复唤醒、无唤醒源和通信进行中 |
| D011-006 | P1 | sh3673510_control_sleep() 忽略关 MOS/均衡/唤醒配置错误；wake() 在运行配置/保护恢复成功之前已清 s_afe_sleeping | 全过程错误传播；失败保持 RUN/未就绪；配置失败后的下次调用不得绕过恢复 |
| D011-007 | P1 | RN4 原图 10M，软件按 10k NTC；RN3 标 TS3-NC 又备注加热 MOS 旁，软件忽略 | 确认 BOM/原图修订，建立真实通道表和温度保护；覆盖开路/短路/错装/加热 MOS 过温 |
| D011-008 | P1→代码修复/待实板 | 旧代码按关 MOS 后的零电流清 SC，会形成持续短路下反复重开风险 | SC 改为独立锁存；必须观察 AFE LOADOFF 连续约 2 s 才尝试清 SC，并在后续读回确认 SC 已消失后才解除放电禁止 |
| D011-009 | P1 | SC 目前采用固定 multiplier=0/delay=7，注释称 2*OCD2/256us；未见产品签核。UV 使用 /5 向下取整；部分电流阈值超范围会钳位 | 补精确手册/参数来源，确认所有量化方向、范围拒绝及延时；SCD 独立验证，不能把现默认值视为量产要求 |
| D011-010 | P1 | sh3673520.* 共用名字与 SH3673510 实际型号不同；原理图不证明寄存器/FLAG/WDT/RESET 相容 | 建立已用寄存器/位/CRC/时序与精确 SH3673510 手册/官方 Demo 的对应表 |
| D011-011 | P1 | 数据已进入通用 BMS 结构，但有旧 CHG_IN_PIN、RF_EN_PIN、MCU_LDO_PIN/AFE1_PRO_EN_PIN 别名 | 逐个追踪运行模式、SOC、保护、Flash、OTA、BLE 与 Modbus 调用者；禁止把旧别名当真实电路功能 |
| D011-012 | P2 | 缺少 D011 断线检测闭环、完整初始化失败诊断、通信故障注入与持久化状态记录 | 增加有依据的断线/畸形帧/掉电/重复初始化测试与诊断记录，不复制旧 AFE 业务 |

本次只修复已确认的纯算术/长度问题并整理资料；上述保护、MOS、供电、加热与休眠问题未在本次静默改动。后续实现需先确定相应硬件/产品语义。

## 5. 实板验证矩阵（均未获得完成证据）

| 测试组 | 最少覆盖 |
|---|---|
| 供电/下载 | 3V3/C-3V3/AFE-VCC，复位默认 IO，SWS 下载，BOM 选项与 250uOhm 实测 |
| SPI/AFE | CS、SCLK、MOSI/MISO、CRC/ACK、超时、拔断、复位、RESET/ALARM 电气与首次通信 |
| 电芯 | 10S 全通道、上下限、上部短接通道排除、断线与瞬态噪声 |
| 电流 | 零点、正负方向、小电流、大电流范围、温漂，SOC 积分方向 |
| 温度 | RN3/RN4 BOM 纠正，TS1/TS2/TS4 冷热、开短路，加热器温度链 |
| 保护/MOS | 逐项 OV/UV/OCD/OCC/SC/温度触发、延时、实际栅极、锁存与真实故障移除恢复 |
| 加热/F1 | PB4/PB5 组合、功率路径、停止条件、失联/测温失败/复位安全态 |
| 均衡 | 各通道、相邻通道限制、时限、温升、采样干扰、停止与失败 |
| 通信 | C-3V3、RS485 DE 与最后停止位、DMA 并发、错帧/长帧、PD3 唤醒、BLE/Modbus 全协议 |
| 低功耗 | 所有 wake 源已有效/边沿变化，BLE 各状态，事务中禁止 sleep，重复睡醒，AFE 恢复失败 |
| 存储/OTA | 旧参数加载与升级、掉电、持久化一致性、OTA 边界、配对/MAC/校准区保护 |

首次功率验证使用电池模拟器、限流电源、电子负载和逻辑分析仪。记录板号、BOM、固件 SHA、配置、仪器、波形与结果。尚无上述证据时，不提供“全部完成”百分比或量产通过结论。

## 6. 推荐执行顺序

1. 先确认 RN3/RN4、PB4/PB5/F1 与通信供电等硬件缺口，并补 SH3673510 手册来源。
2. 修复 MOS 请求/测量有效性/错误许可、重复 GPIO 写入者与低功耗错误传播；增加可执行的状态机和故障注入测试。
3. 完成 RS485 供电/唤醒与 10S 采样、SOC、协议、存储/OTA 集成回归。
4. 执行远端 TC32 全门禁和上表实板矩阵，关闭对应 ID 后更新文档。
