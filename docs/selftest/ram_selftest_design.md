# RAM 与栈自检设计

启动和运行测试都限定在链接可见的 64×32 位专用 RAM 数组，不破坏 BLE 栈、AFE 数据或应用变量。启动采用 March-like 0/1/0 顺逆序读写；运行时每周期只处理 4 个字，先保存、测试、再恢复，并校验关键协调状态的正反码。

栈测试由链接器 NOLOAD guard、启动填充和运行水位/guard 检查组成；链接器仍保留原工程最小栈余量断言。覆盖边界是专用 RAM、guard 和关键冗余变量，不是整片 64 KiB RAM；不能据此宣称完整 RAM Class B 覆盖。
