本版本DemoCode适用于DVC11XX-D系列芯片

芯片在上电后的状态有可能是SHUTDOWN NORMAL SLEEP，初始化前需先进行IIC唤醒，即SCL置“1”、SDA置“0”

芯片的V3P3输出默认在休眠状态下为关闭，需手动开启，配置寄存器位“V3P3ES”为“1”

芯片的电池欠压看门狗屏蔽默认在状态下为开启，需手动关闭，配置寄存器位“CUWM”为“1”

芯片的CADC计算屏蔽在默认状态下为开启，需手动关闭，配置寄存器R142为0x80
