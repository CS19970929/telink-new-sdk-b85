# SOC 模块逻辑梳理与优化说明

## 目标

本次调整聚焦两个原则：

1. SOC 校准一次最多只变动 `1%`
2. 非充电场景不允许 SOC 向上校准

同时清理了 SOC 模块中不再使用的宏、变量和旧校准路径，保留当前真正在线运行的策略链路。

## 当前运行链路

- `APP_SOC_IntEnhance_Ctrl()` 作为主入口，每个周期执行一次 SOC 策略
- `soc_strategy_update()` 负责策略仲裁
- `SOC_Result_Pass()` 负责把结果同步到上层报告结构

## 现有策略顺序

`soc_strategy_update()` 现在按以下顺序处理：

1. 启动阶段 OCV 纠偏
2. 充满/充空端点同步
3. 放电末端 SOC 上限跟踪
4. 静置 OCV 跟踪

其中每次调用只允许命中一个校准动作，命中后立即返回，不再叠加第二个动作。

## 关键规则

- 启动 OCV 纠偏只允许向下修正到 `0%`
- 静置 OCV 跟踪只允许向下修正
- 放电末端跟踪只允许向下修正
- 充满同步允许向上修正，因为这是充电场景
- 充空同步只允许向下修正
- 任意一次策略调用中，SOC 变化步进固定为 `1%`

## 已删除的废弃项

- 旧的 OCV 映射接口
- 终端校准旧路径
- EEPROM 相关的 SOC 监控/修正逻辑
- SOC 备份结构体
- 不再使用的配置宏和初始化变量

## 说明

本次没有加入持久化节流逻辑，按当前要求保持原有 KV 写入节奏不变。

## 新增放电 OCV 对比修正

- 在放电态且安时积分正常运行时，增加按放电电流分档的 OCV 对比修正
- 低电流场景允许更积极地向下修正，高电流场景更保守，避免负载压降带来的误判
- 只允许向下修正，不允许在放电场景向上修正
- 单次修正步进仍然限制为 `1%`
- 当 OCV 差值未达到阈值时，不累计修正动作，减少边界抖动
